# Django
SECRET_KEY=change_me_to_a_random_long_string
DEBUG=False
DJANGO_ALLOWED_HOSTS=sglci.sajholding.org

# Database (PostgreSQL)
DB_NAME=loqt
DB_USER=loqt
DB_PASSWORD=loqt264
DB_HOST=localhost
DB_PORT=5432

# Email (optional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True

# Any other env vars your project expects
